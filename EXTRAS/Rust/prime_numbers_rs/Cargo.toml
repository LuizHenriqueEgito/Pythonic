[package]
name = "prime_numbers_rs"
version = "0.1.0"
edition = "2024"

[lib]
name = "prime_numbers_fn"  # Deve ser o mesmo nome do seu cargo init ___
crate-type = ["cdylib"]  # TODO: Pra que serve esse cara?

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
rayon = "1.8"